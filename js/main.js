document.addEventListener('DOMContentLoaded', () => {
    // Main JS initialization
    console.log('AdminPortal initialized');

    // Future: Add Mobile Menu Toggle logic here if needed
    // currently sidebar is fixed w-64, responsivness is handled via CSS classes in sidebar.js/index.html
});
